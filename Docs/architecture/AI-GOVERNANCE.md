# Governança de IA

Nenhum módulo chama provedor diretamente. Gateway é esqueleto.

Proibido nesta fundação: chamadas externas, treino com dados de cliente, telemetria de prompt.

Estados de `AIArtifact` em `packages/domain`. Publicação exige humano.
