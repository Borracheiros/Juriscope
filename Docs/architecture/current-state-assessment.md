# Avaliação do estado atual

**Classificação:** GREENFIELD

O repositório continha apenas governança Cursor (`.cursor/rules`, `.cursor/skills`) e a EAP acadêmica em `Docs/`. Não havia `package.json`, aplicação, banco, CI nem `AGENTS.md`.

O Git local foi inicializado em `E:/TCC/Juridico-IA` para não usar o repositório do drive `E:/` (ownership duvidoso).

A rule copiada de ambiente Railway compartilhado **não se aplica** a esta fundação: testes usam Postgres descartável; Compose local só para desenvolvimento. Produção/Railway fica fora do Sprint 0.

## Riscos herdados

- Conteúdo de rules/skills ainda cita fluxos clínicos de outro produto; domínio jurídico prevalece neste código.
- Sem histórico Git anterior a esta fundação.
