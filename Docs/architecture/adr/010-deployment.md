# ADR 010 — Deployment

Containers + Compose para Postgres local. CI no GitHub Actions. Sem deploy/push nesta rodada. Staging obrigatório antes de produção futura. Feature flags fail-closed quando existirem.
