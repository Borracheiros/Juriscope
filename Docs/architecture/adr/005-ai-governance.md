# ADR 005 — Governança de IA

IA não confirma prazo, não limpa conflito, não publica artefato, não protocola. Model Gateway único (esqueleto). RAG com ACL no retrieval. Resposta sem fonte = `SOURCE_INCOMPLETE`. Zero chamadas a provedor nesta fundação.
